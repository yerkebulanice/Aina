import 'package:flutter/material.dart';

class DrawerButtons {
  final String name;
  final IconData icon;
  bool? isSelected;

  DrawerButtons({required this.name, required this.icon, this.isSelected});
}
